# Simple Routing

- Simple Routing supports one record per DNS name.
- Each record can have multiple values, all values are returned in random order. Client choose one of the values to connect to.
- Use Simple Routing when you want to route traffic to a single resource.
- Simple Routing does not support health checks.
